import { DropDownNavigateDirective } from './drop-down-navigate.directive';

describe('DropDownNavigateDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDownNavigateDirective();
    expect(directive).toBeTruthy();
  });
});
